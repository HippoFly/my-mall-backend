spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
        namespace: mall
        group: dev
      config:
        namespace: mall
        server-addr: http://localhost:8848
        file-extension: yaml
        group: dev