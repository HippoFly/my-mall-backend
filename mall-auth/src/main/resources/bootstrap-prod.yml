spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://nacos:8848
        namespace: mall
        group: prod
      config:
        server-addr: http://nacos:8848
        file-extension: yaml
        namespace: mall
        group: prod
logging:
  level:
    root: info