spring:
  cloud:
    nacos:
      discovery:
        namespace: mall
        server-addr: http://nacos:8848
        group: prod
      config:
        namespace: mall
        server-addr: http://nacos:8848
        file-extension: yaml
        group: prod