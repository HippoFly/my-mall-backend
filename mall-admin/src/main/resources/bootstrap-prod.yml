spring:
  cloud:
    nacos:
      discovery:
        namespace: mall
        group: prod
        server-addr: http://nacos:8848
      config:
        namespace: mall
        group: prod
        file-extension: yaml
        server-addr: http://nacos:8848
